{
  "name": "demoblaze-e2e-tests",
  "version": "1.0.0",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "setup": "tsx scripts/setupAndSignup.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.0",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "@faker-js/faker": "^9.8.0"
  }
}
